function sayHelloBye(name, num) {
  return num
    ? `Hello ${name[0].toUpperCase() + name.slice(1)}`
    : `Bye ${name[0].toUpperCase() + name.slice(1)}`;
}

// Problem link: https://edabit.com/challenge/kLa2w7m4h849k8rmW
